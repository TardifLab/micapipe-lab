# Central MICAPIPE codebase for Tardiflab

This core package remains linked to the official repo (upstream).

Below are general notes.

# Authentication

Ideally, we separate development/pushing from running jobs. Only those involved in development of the core codebase should have permission to push changes.

Each developer will require their own SSH key, which ensures that changes are tied to specific accounts.
